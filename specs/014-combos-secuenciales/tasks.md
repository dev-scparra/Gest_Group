# Tasks 014 — Combos Secuenciales

**Depende de:** [spec.md](./spec.md), [plan.md](./plan.md), y de 001/002/006/009 (ya implementados).

- [x] **T014-01** `src/algebra/combinador.py`: `ResultadoCombinacion` (dataclass) + `CombinadorGestos` (`__init__`, `actualizar`, `reset`), según `plan.md` Sección 2. Depende de 001 (`operacion_G`).
- [x] **T014-02** `tests/test_combinador.py`: combo dentro de ventana (CMB-FR-001); sin combo por ventana expirada (CMB-FR-002); sin pendiente previo; `E` limpia el pendiente y nunca combina, en ambas posiciones (CMB-FR-003); el pendiente tras un combo es el gesto crudo — caso `G1→G3→G2` de `spec.md` Sección 4 (CMB-FR-004); `ventana_s` custom (CMB-FR-005); determinismo sin reloj real (CMB-FR-006). 10 tests en verde. Depende de T014-01.
- [x] **T014-03** `config/default.yaml`: añadir clave `combinador.ventana_s: 1.5`.
- [x] **T014-04** `src/main.py`, `cargar_config()`: añadir `"combinador"` a `claves_requeridas` (INT-FR-001).
- [x] **T014-05** `tests/test_config.py`: caso `combinador.ventana_s` faltante → `ValueError` explícito, más aserción de tipo/rango en el test de carga real. Depende de T014-04.
- [x] **T014-06** `src/main.py`: instanciar `combinador = CombinadorGestos(ventana_s=config["combinador"]["ventana_s"])` en `main()`; añadir `combinador` (y `ahora: float`) como parámetros de `procesar_frame()`; aplicar φ sobre `combinador.actualizar(gesto_confirmado, ahora).gesto_efectivo` (INT-FR-010). `main()` pasa `time.time()`. Depende de T014-01.
- [x] **T014-07** `tests/test_orquestacion.py`: caso end-to-end — dos confirmaciones con paso de reloj corto (0.02s/frame, dentro de `ventana_s`) producen `φ(G1∘G3)=φ(G4)=A4`; los mismos gestos con paso grande (100s/frame, fuera de ventana) producen `A3` (sin regresión). Se actualizó `_correr`/`piezas` para inyectar el reloj simulado. Depende de T014-06.
- [x] **T014-08 [P2]** `src/visualizacion/renderer.py`: `dibujar_frame(..., combo: ResultadoCombinacion | None = None)` — línea de overlay `combo con {previo} -> {efectivo}` cuando `combo.es_combo` (VIS-FR-009 de 008/spec.md). `ResultadoCombinacion` no guarda el g2 crudo, así que la línea usa solo `gesto_previo` y `gesto_efectivo`. Depende de T014-06.
- [x] **T014-09 [P2]** `tests/test_renderer.py`: combo real → la línea aparece (más píxeles); `es_combo=False` → no aparece; `combo=None` → no lanza excepción (comportamiento actual preservado).
- [ ] **T014-10** Verificación de campo (requiere cámara): calibrar `ventana_s` — ejecutar 2-3 combos intencionales (p. ej. `G1` luego `G3` en menos de 1.5s) y confirmar que disparan `φ(G1∘G3)=φ(G4)`; hacer gestos aislados con >2s de separación y confirmar que **no** disparan combos espurios. Ajustar `ventana_s` en `config/default.yaml` si el valor por defecto se siente demasiado corto (combos intencionales fallan) o demasiado largo (combos accidentales frecuentes). **Pendiente — requiere cámara.**
- [x] **T014-11** `docs/teoria_grupos_aplicada.md` (Sección 0, aclaración 1): actualizada para reflejar que desde 014 `operacion_G` tiene un segundo consumidor en vivo (`CombinadorGestos`). `docs/axiomas_de_grupo_explicados.md`: nueva Sección 8 "Los combos: cuando `∘` se usa de verdad, en vivo" + ajuste de la nota de asociatividad. `docs/guion_video.md`: revisado, no contenía la afirmación obsoleta.
- [x] **T014-12** `README.md`: nueva subsección "Combos secuenciales — la composición `∘` en vivo" (qué es, `ventana_s`, ejemplo `G1∘G3=G4`); conteo de tests actualizado a 102; menciones de config y estructura actualizadas.

**Definición de hecho:** `pytest` completo en verde (102 tests, incluidos `test_combinador.py`, los dos casos de combo de `test_orquestacion.py`, `test_config.py` y `test_pureza_algebra.py` que cubre `combinador.py`). ✅ **Cumplido salvo T014-10** (verificación con cámara real, pendiente de hardware). La documentación de `docs/` ya no afirma que `operacion_G` solo se usa offline.
